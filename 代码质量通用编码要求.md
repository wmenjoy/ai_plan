# 代码质量通用编程要求

## 1. 目的与范围

本文档旨在建立公司范围内的代码质量标准，确保所有开发团队遵循一致的编程实践，提高代码的可读性、可维护性、安全性和性能。这些要求适用于所有编程语言和项目类型，可根据具体项目需求进行适当调整。

## 2. 代码质量核心原则

### 2.1 代码可读性
- 代码应当遵循项目和语言的标准约定，提高团队协作效率
- 命名应当准确反映变量、函数、类的用途和意图
- 代码结构应当逻辑清晰，避免深层嵌套和复杂逻辑

### 2.2 代码可维护性
- 组件间应保持低耦合，任何模块的变更对其他模块的影响应最小化
- 组件内部应当保持高内聚性，相关功能应组织在一起
- 代码应当有合理的模块化设计，便于重用和扩展

### 2.3 代码健壮性
- 代码应当能够妥善处理所有可预见的异常情况
- 边界条件和异常路径应当得到全面测试
- 具备自动化测试覆盖核心业务逻辑

### 2.4 代码安全性
- 代码应当遵循行业安全编码标准
- 应采取措施防止已知的安全漏洞
- 敏感数据应当加密存储和传输

### 2.5 代码性能
- 代码应当高效利用系统资源，满足规定的性能指标
- 关键路径上的算法应当经过优化
- 应避免不必要的计算和资源消耗

## 3. 详细要求

### 3.1 命名规范

#### 3.1.1 通用命名原则
- 名称应当准确描述其所表示的内容，审查者阅读时无需额外解释
- 除广泛接受的缩写(如HTTP)外，禁止使用自创缩写
- 禁止使用模糊或太宽泛的名称(如data, info, manager等)
- 项目中的命名风格必须一致(如驼峰式、下划线式等)

**示例：命名原则的应用**

```java
// 不良实践：名称模糊，使用自创缩写
void procTx(Object d) {
    // 处理交易数据
}

// 良好实践：名称清晰明确
void processTransaction(TransactionData transactionData) {
    // 处理交易数据
}

// 不良实践：过于宽泛的名称
class Manager {
    // 管理什么？不明确
}

// 良好实践：明确表达职责
class UserAccountManager {
    // 管理用户账户
}
```

#### 3.1.2 具体命名要求
- **变量命名**：使用名词或形容词+名词，长度控制在2-20个字符内
- **函数命名**：使用动词或动词+名词，明确表示其执行的动作，长度控制在2-30个字符内
- **类命名**：使用名词，表示其代表的实体，长度控制在2-30个字符内
- **常量命名**：使用全大写字母，单词间用下划线分隔，描述其用途
- **布尔变量命名**：必须使用is, has, can等前缀，清晰表明其布尔性质

**示例：各类命名规范**

```typescript
// 变量命名示例
const activeUser = getUserById(1001);       // 良好：形容词+名词
const userList = fetchAllUsers();           // 良好：名词+名词
const x = calculateDistance(point1, point2); // 不良：无意义的单字母名称

// 函数命名示例
function validateEmail(email: string): boolean { /* ... */ }  // 良好：动词+名词
function updateUserProfile(user: User): void { /* ... */ }    // 良好：动词+名词
function process() { /* ... */ }                             // 不良：过于宽泛

// 类命名示例
class PaymentProcessor { /* ... */ }        // 良好：名词，表示实体
class OrderConfirmation { /* ... */ }       // 良好：明确表达职责
class Mgr { /* ... */ }                     // 不良：缩写且不明确

// 常量命名示例
const MAX_RETRY_ATTEMPTS = 3;               // 良好：全大写下划线分隔
const DEFAULT_TIMEOUT_MS = 5000;            // 良好：含单位信息
const config = { timeoutMs: 3000 };         // 不良：常量应大写

// 布尔变量命名示例
const isActive = user.status === 'active';  // 良好：is前缀
const hasPermission = checkAccess(user, resource); // 良好：has前缀
const canEdit = user.role === 'editor';     // 良好：can前缀
const valid = validateInput(input);         // 不良：缺少布尔前缀
```

### 3.2 代码结构与组织

#### 3.2.1 文件组织
- 每个文件必须只有一个主要职责
- 文件长度必须控制在500行以内，超过需要分解或提供合理说明
- 相关功能必须组织在同一模块或包中
- 文件命名必须反映其包含的主要类或功能

**示例：合理的文件组织**

```
src/
├── auth/                  // 认证模块
│   ├── AuthService.ts     // 认证服务
│   ├── AuthController.ts  // 认证控制器
│   └── models/            // 认证相关模型
│       ├── User.ts
│       └── Permission.ts
├── payment/               // 支付模块
│   ├── PaymentService.ts
│   ├── PaymentController.ts
│   └── models/
│       ├── Transaction.ts
│       └── PaymentMethod.ts
└── common/                // 通用功能
    ├── utils/
    │   ├── DateUtils.ts
    │   └── StringUtils.ts
    └── exceptions/
        └── AppException.ts
```

#### 3.2.2 函数设计
- 函数必须只实现一个清晰定义的功能
- 函数长度必须控制在50行以内，超过需要分解或提供合理说明
- 函数参数数量必须不超过5个，超过需使用结构体/对象封装
- 禁止使用全局变量传递数据，必须通过参数传递

**示例：函数设计的好与坏**

```javascript
// 不良实践：函数过长，做了多件事情
function processOrder(orderId) {
    // 50+ 行代码，包含：
    // 1. 验证订单
    // 2. 计算总价
    // 3. 验证库存
    // 4. 处理支付
    // 5. 发送确认邮件
}

// 良好实践：函数职责单一，拆分为多个小函数
function processOrder(orderId) {
    const order = validateOrder(orderId);
    const totalPrice = calculateTotalPrice(order);
    verifyInventory(order.items);
    processPayment(order, totalPrice);
    sendConfirmationEmail(order);
}

// 不良实践：参数过多
function createUser(firstName, lastName, email, phone, address, city, country, birthDate) {
    // 创建用户...
}

// 良好实践：使用对象封装参数
function createUser(userInfo) {
    // userInfo 包含 {firstName, lastName, email, ...} 
    // 创建用户...
}
```

#### 3.2.3 类设计
- 类必须具有单一职责，公共方法数量应控制在20个以内
- 类的公共接口必须有明确文档说明
- 类之间的继承层次不应超过3层
- 优先使用组合而非继承，继承关系必须符合"是一个"的语义

**示例：类设计原则**

```python
# 不良实践：类职责不单一，方法过多
class UserManager:
    def create_user(self): pass
    def update_user(self): pass
    def delete_user(self): pass
    def find_user(self): pass
    def authenticate_user(self): pass  # 认证功能不应在此类中
    def authorize_user(self): pass     # 授权功能不应在此类中
    def send_welcome_email(self): pass # 邮件功能不应在此类中
    # ...20+方法

# 良好实践：职责单一，方法数量合理
class UserManager:
    def create_user(self): pass
    def update_user(self): pass
    def delete_user(self): pass
    def find_user(self): pass

class AuthService:
    def authenticate_user(self): pass
    def authorize_user(self): pass

class EmailService:
    def send_welcome_email(self): pass

# 不良实践：继承层次过深
class Vehicle: pass
class MotorVehicle(Vehicle): pass
class Car(MotorVehicle): pass
class SportsCar(Car): pass
class Ferrari(SportsCar): pass  # 继承层次太深

# 良好实践：优先使用组合而非继承
class Engine:
    def start(self): pass

class Vehicle:
    def __init__(self, engine):
        self.engine = engine  # 组合
    
    def start_engine(self):
        self.engine.start()
```

### 3.3 注释和文档

#### 3.3.1 注释原则
- 复杂算法必须有伪代码或流程说明
- 业务逻辑必须注明需求来源或规则依据
- 注释必须与代码同步更新，过期注释必须删除
- 注释率(注释行/代码行)应保持在10%-30%之间

**示例：良好的注释实践**

```java
/**
 * 计算贷款月还款额
 * 使用等额本息还款法(需求文档：REQ-2023-042)
 * 
 * 公式: M = P * (i * (1 + i)^n) / ((1 + i)^n - 1)
 * 其中:
 * M = 月还款额
 * P = 贷款本金
 * i = 月利率(年利率/12)
 * n = 贷款期限(月数)
 * 
 * 时间复杂度: O(1)
 */
public double calculateMonthlyPayment(double principal, double annualRate, int termInMonths) {
    // 将年利率转换为月利率
    double monthlyRate = annualRate / 12.0 / 100.0;
    
    // 使用还款公式计算月还款额
    double monthlyPayment = principal * 
                           (monthlyRate * Math.pow(1 + monthlyRate, termInMonths)) / 
                           (Math.pow(1 + monthlyRate, termInMonths) - 1);
    
    return monthlyPayment;
}
```

#### 3.3.2 文档要求
- 所有公共API必须有完整文档，包含：用途、参数、返回值、异常、使用示例
- 每个模块必须有README文件说明其功能、依赖和使用方法
- 架构设计文档必须包含：组件关系图、数据流图、部署图
- 技术方案文档必须包含：需求分析、解决方案、技术选型理由、风险分析

**示例：API文档**

```typescript
/**
 * 用户认证服务
 * 
 * 处理用户登录、注册和认证相关功能。
 */
export class AuthService {
  /**
   * 使用邮箱和密码验证用户身份
   * 
   * @param email - 用户邮箱地址
   * @param password - 用户密码(明文)
   * @returns 包含用户信息和访问令牌的对象
   * @throws AuthenticationError - 当认证失败时抛出(邮箱不存在或密码错误)
   * @throws ValidationError - 当输入参数格式不正确时抛出
   * 
   * @example
   * ```typescript
   * try {
   *   const authResult = await authService.authenticate('user@example.com', 'password123');
   *   console.log(`用户 ${authResult.user.name} 成功登录`);
   * } catch (error) {
   *   console.error('登录失败:', error.message);
   * }
   * ```
   */
  async authenticate(email: string, password: string): Promise<AuthResult> {
    // 实现逻辑
  }
}
```

### 3.4 错误处理

#### 3.4.1 异常管理
- 必须捕获并处理所有可预见的异常
- 禁止空catch块，必须至少记录日志
- 自定义异常必须包含：错误码、错误信息、发生位置、上下文信息
- 错误信息必须对最终用户友好，不暴露系统内部细节

**示例：异常处理**

```java
// 自定义异常类
public class ServiceException extends RuntimeException {
    private final String errorCode;
    private final String component;
    private final Map<String, Object> context;

    public ServiceException(String errorCode, String message, String component, Map<String, Object> context) {
        super(message);
        this.errorCode = errorCode;
        this.component = component;
        this.context = context;
    }
    
    // Getters...
}

// 良好实践：异常处理
public UserDTO getUserById(Long userId) {
    try {
        User user = userRepository.findById(userId)
            .orElseThrow(() -> new EntityNotFoundException("User not found with ID: " + userId));
        return userMapper.toDTO(user);
    } catch (EntityNotFoundException e) {
        // 记录特定异常，并向上抛出业务异常
        logger.warn("User lookup failed: {}", e.getMessage());
        Map<String, Object> context = Map.of("userId", userId);
        throw new ServiceException("USER_NOT_FOUND", "找不到指定的用户", "UserService", context);
    } catch (DataAccessException e) {
        // 记录数据库异常，转换为友好的业务异常
        logger.error("Database error during user lookup: {}", e.getMessage(), e);
        Map<String, Object> context = Map.of("userId", userId, "operation", "findById");
        throw new ServiceException("DATABASE_ERROR", "系统暂时无法访问用户数据", "UserService", context);
    }
}

// 不良实践：空catch块
public User getUserById(Long userId) {
    try {
        return userRepository.findById(userId).orElse(null);
    } catch (Exception e) {
        // 空catch块，吞噬异常
    }
    return null;
}

// 不良实践：过于宽泛的异常捕获
public UserDTO getUserById(Long userId) {
    try {
        // 数据库操作
    } catch (Exception e) {  // 捕获所有异常，无法区分不同类型的错误
        logger.error("Error:", e);
        throw new RuntimeException("An error occurred");  // 丢失原始异常信息
    }
}
```

#### 3.4.2 防御性编程
- 所有外部输入必须进行类型、范围、格式验证
- 所有数组访问必须检查索引边界
- 所有指针/引用使用前必须检查是否为空
- 资源分配必须在try-finally或类似结构中确保释放

**示例：防御性编程**

```csharp
// 良好实践：输入验证
public void ProcessUserData(UserInputDTO input)
{
    // 参数验证
    if (input == null)
    {
        throw new ArgumentNullException(nameof(input), "用户输入不能为空");
    }
    
    if (string.IsNullOrEmpty(input.Email))
    {
        throw new ArgumentException("邮箱地址不能为空", nameof(input.Email));
    }
    
    if (!Regex.IsMatch(input.Email, @"^[^@\s]+@[^@\s]+\.[^@\s]+$"))
    {
        throw new ArgumentException("邮箱地址格式不正确", nameof(input.Email));
    }
    
    if (input.Age < 18 || input.Age > 120)
    {
        throw new ArgumentOutOfRangeException(nameof(input.Age), "年龄必须在18到120之间");
    }
    
    // 继续处理...
}

// 良好实践：资源管理
public String readFirstLineFromFile(String path) {
    try (BufferedReader br = new BufferedReader(new FileReader(path))) {
        return br.readLine();
    } catch (IOException e) {
        logger.error("Failed to read file: {}", path, e);
        throw new ServiceException("FILE_READ_ERROR", "无法读取文件", "FileService", 
                                  Map.of("path", path));
    }
}

// 不良实践：资源未正确释放
public String readFirstLineFromFile(String path) {
    BufferedReader br = null;
    try {
        br = new BufferedReader(new FileReader(path));
        return br.readLine();
    } catch (IOException e) {
        logger.error("Error reading file", e);
        return null;
    }
    // 未关闭BufferedReader，可能导致资源泄漏
}
```

### 3.5 测试要求

#### 3.5.1 单元测试
- 每个公共方法必须至少有1个正向测试用例和1个异常测试用例
- 测试用例必须独立且可重复运行，不依赖执行顺序
- 测试数据必须使用测试框架提供的夹具或工厂方法创建
- 禁止测试用例间共享可变状态

**示例：单元测试**

```java
// 被测试的类
public class Calculator {
    public int add(int a, int b) {
        return a + b;
    }
    
    public int divide(int a, int b) {
        if (b == 0) {
            throw new IllegalArgumentException("除数不能为零");
        }
        return a / b;
    }
}

// 单元测试
@Test
public void testAdd_PositiveNumbers_ReturnsCorrectSum() {
    // 准备
    Calculator calculator = new Calculator();
    int a = 5;
    int b = 3;
    
    // 执行
    int result = calculator.add(a, b);
    
    // 验证
    assertEquals(8, result);
}

@Test
public void testAdd_NegativeNumbers_ReturnsCorrectSum() {
    Calculator calculator = new Calculator();
    assertEquals(-8, calculator.add(-5, -3));
}

@Test
public void testDivide_ValidDivision_ReturnsCorrectResult() {
    Calculator calculator = new Calculator();
    assertEquals(2, calculator.divide(10, 5));
}

@Test
public void testDivide_DivisionByZero_ThrowsException() {
    // 准备
    Calculator calculator = new Calculator();
    
    // 执行 & 验证
    Exception exception = assertThrows(IllegalArgumentException.class, () -> {
        calculator.divide(10, 0);
    });
    
    assertEquals("除数不能为零", exception.getMessage());
}
```

#### 3.5.2 测试覆盖率
- 行覆盖率必须达到80%以上
- 分支覆盖率必须达到70%以上
- 核心业务逻辑必须达到90%以上的覆盖率
- 每次代码提交必须通过所有现有测试

### 3.6 性能优化

#### 3.6.1 算法效率
- 循环体内不应有数据库或网络操作
- 时间复杂度必须与数据规模匹配：小数据量(n<1000)可接受O(n²)，大数据量(n>=1000)必须不超过O(n log n)
- 内存使用不应随请求数线性增长
- 关键算法必须有性能基准测试报告

**示例：算法效率优化**

```java
// 不良实践：循环中进行数据库操作
public List<UserDTO> enrichUserData(List<Long> userIds) {
    List<UserDTO> result = new ArrayList<>();
    for (Long userId : userIds) {
        // 在循环中执行数据库查询，效率低下
        User user = userRepository.findById(userId).orElse(null);
        if (user != null) {
            // 在循环中执行另一个数据库查询
            List<Order> orders = orderRepository.findByUserId(userId);
            UserDTO dto = new UserDTO(user);
            dto.setOrderCount(orders.size());
            result.add(dto);
        }
    }
    return result;
}

// 良好实践：批量查询减少数据库操作
public List<UserDTO> enrichUserData(List<Long> userIds) {
    // 一次性批量查询所有用户
    List<User> users = userRepository.findAllByIdIn(userIds);
    
    // 一次性批量查询所有订单
    List<Order> allOrders = orderRepository.findByUserIdIn(userIds);
    
    // 构建用户ID到订单列表的映射
    Map<Long, List<Order>> userOrdersMap = allOrders.stream()
            .collect(Collectors.groupingBy(Order::getUserId));
    
    // 组装结果
    return users.stream()
            .map(user -> {
                UserDTO dto = new UserDTO(user);
                List<Order> userOrders = userOrdersMap.getOrDefault(user.getId(), Collections.emptyList());
                dto.setOrderCount(userOrders.size());
                return dto;
            })
            .collect(Collectors.toList());
}
```

#### 3.6.2 资源管理
- 数据库连接必须在使用后立即关闭或返回连接池
- 大对象必须在不需要时显式释放
- 缓存使用必须有明确的过期策略
- 并发访问必须使用合适的锁策略或无锁算法

**示例：资源管理**

```java
// 良好实践：使用连接池和自动关闭资源
@Configuration
public class DatabaseConfig {
    @Bean
    public DataSource dataSource() {
        HikariConfig config = new HikariConfig();
        config.setJdbcUrl("jdbc:mysql://localhost:3306/mydb");
        config.setUsername("user");
        config.setPassword("password");
        config.setMaximumPoolSize(10);
        config.setMinimumIdle(5);
        config.setIdleTimeout(30000);
        config.setConnectionTimeout(2000);
        return new HikariDataSource(config);
    }
}

// 良好实践：缓存使用
@Service
public class ProductService {
    private final ProductRepository productRepository;
    private final Cache<Long, Product> productCache;

    public ProductService(ProductRepository productRepository) {
        this.productRepository = productRepository;
        // 配置缓存：最大容量100，写入后15分钟过期
        this.productCache = Caffeine.newBuilder()
                .maximumSize(100)
                .expireAfterWrite(15, TimeUnit.MINUTES)
                .recordStats()
                .build();
    }

    public Product getProductById(Long id) {
        return productCache.get(id, key -> productRepository.findById(key)
                .orElseThrow(() -> new EntityNotFoundException("Product not found")));
    }
    
    public void clearCache() {
        productCache.invalidateAll();
    }
}
```

### 3.7 安全编码

#### 3.7.1 输入验证
- 所有用户输入必须进行验证，不可信数据必须经过清洗
- SQL查询必须使用参数化查询，禁止字符串拼接
- 所有文件路径操作必须进行规范化和安全检查
- 所有执行系统命令的输入必须进行命令注入检查

**示例：安全的输入处理**

```java
// 不良实践：SQL注入风险
public User findUserByUsername(String username) {
    // 危险：直接拼接SQL字符串
    String sql = "SELECT * FROM users WHERE username = '" + username + "'";
    try (Statement stmt = connection.createStatement();
         ResultSet rs = stmt.executeQuery(sql)) {
        if (rs.next()) {
            return mapResultSetToUser(rs);
        }
    } catch (SQLException e) {
        logger.error("数据库查询错误", e);
    }
    return null;
}

// 良好实践：参数化查询防止SQL注入
public User findUserByUsername(String username) {
    String sql = "SELECT * FROM users WHERE username = ?";
    try (PreparedStatement stmt = connection.prepareStatement(sql)) {
        stmt.setString(1, username);
        try (ResultSet rs = stmt.executeQuery()) {
            if (rs.next()) {
                return mapResultSetToUser(rs);
            }
        }
    } catch (SQLException e) {
        logger.error("数据库查询错误", e);
    }
    return null;
}

// 不良实践：路径遍历风险
public String readFileContent(String fileName) {
    // 危险：未验证的文件路径
    File file = new File("user_files/" + fileName);
    // 读取文件...
}

// 良好实践：安全的文件路径处理
public String readFileContent(String fileName) {
    // 验证文件名不包含路径遍历序列
    if (fileName.contains("..") || fileName.contains("/") || fileName.contains("\\")) {
        throw new IllegalArgumentException("Invalid file name");
    }
    
    // 规范化和限制路径在安全目录内
    Path basePath = Paths.get("user_files").toAbsolutePath().normalize();
    Path filePath = basePath.resolve(fileName).normalize();
    
    if (!filePath.startsWith(basePath)) {
        throw new SecurityException("Attempted path traversal");
    }
    
    // 读取文件...
}
```

#### 3.7.2 数据保护
- 所有密码必须使用不可逆加密(如bcrypt)存储，最小工作因子为10
- 敏感数据(如信用卡)必须使用强加密(如AES-256)存储
- 传输中的敏感数据必须使用TLS 1.2+加密
- 禁止在日志中记录密码、令牌等敏感信息，即使是加密形式

**示例：安全的数据处理**

```java
// 不良实践：不安全的密码存储
public void createUser(String username, String password) {
    // 危险：明文存储密码
    String sql = "INSERT INTO users (username, password) VALUES (?, ?)";
    try (PreparedStatement stmt = connection.prepareStatement(sql)) {
        stmt.setString(1, username);
        stmt.setString(2, password);  // 明文密码
        stmt.executeUpdate();
    } catch (SQLException e) {
        logger.error("创建用户失败", e);
    }
}

// 良好实践：安全的密码存储
public void createUser(String username, String password) {
    // 使用BCrypt加密密码，工作因子为12
    String hashedPassword = BCrypt.hashpw(password, BCrypt.gensalt(12));
    
    String sql = "INSERT INTO users (username, password) VALUES (?, ?)";
    try (PreparedStatement stmt = connection.prepareStatement(sql)) {
        stmt.setString(1, username);
        stmt.setString(2, hashedPassword);  // 加密后的密码
        stmt.executeUpdate();
    } catch (SQLException e) {
        logger.error("创建用户失败", e);
    }
}

// 良好实践：敏感数据加密
public void storePaymentInfo(String userId, CreditCard creditCard) {
    // 加密信用卡信息
    String encryptedCardNumber = encryptData(creditCard.getCardNumber());
    
    // 只存储必要信息，如卡号后四位用于显示
    String last4Digits = creditCard.getCardNumber().substring(creditCard.getCardNumber().length() - 4);
    
    String sql = "INSERT INTO payment_methods (user_id, card_type, last_four, encrypted_number) VALUES (?, ?, ?, ?)";
    try (PreparedStatement stmt = connection.prepareStatement(sql)) {
        stmt.setString(1, userId);
        stmt.setString(2, creditCard.getCardType());
        stmt.setString(3, last4Digits);
        stmt.setString(4, encryptedCardNumber);
        stmt.executeUpdate();
    } catch (SQLException e) {
        logger.error("存储支付信息失败", e);
    }
}

// 良好实践：安全日志记录
public void logLoginAttempt(String username, boolean success) {
    logger.info("Login attempt for user: {}, status: {}", 
        username, 
        success ? "SUCCESS" : "FAILURE");
}

// 不良实践：在日志中记录敏感信息
public void logLoginAttempt(String username, String password, boolean success) {
    // 危险：在日志中记录密码
    logger.info("Login attempt - user: {}, password: {}, status: {}", 
        username, 
        password,  // 不应记录密码
        success ? "SUCCESS" : "FAILURE");
}
```

### 3.8 版本控制与变更管理

#### 3.8.1 提交实践
- 每次提交必须只包含一个逻辑变更
- 提交消息必须遵循统一格式：`类型(范围): 简短描述`
- 提交前必须通过所有自动化测试
- 禁止提交编译不通过的代码或注释掉的大块代码

**示例：良好的提交实践**

```bash
# 不良实践：提交消息不明确
git commit -m "修复bug"

# 良好实践：明确的提交格式
git commit -m "fix(auth): 修复登录页面token过期不跳转的问题"
git commit -m "feat(payment): 新增支付宝支付方式"
git commit -m "refactor(api): 重构用户API以提高性能"
git commit -m "docs(readme): 更新安装指南和API文档"
git commit -m "test(user): 添加用户注册流程的集成测试"

# 不良实践：一次提交包含多个不相关变更
git add .
git commit -m "实现登录功能、修复首页样式、更新readme"

# 良好实践：拆分为多个逻辑独立的提交
git add src/auth/*
git commit -m "feat(auth): 实现用户登录功能"

git add src/home/styles.css
git commit -m "fix(home): 修复首页在移动设备上的显示问题"

git add README.md
git commit -m "docs(readme): 更新项目说明和使用方法"
```

#### 3.8.2 分支策略
- 主分支(main/master)必须随时可发布
- 功能开发必须在特性分支进行
- 分支生存周期不应超过2周
- 合并到主分支前必须通过代码审查和自动化测试

**示例：Git分支工作流**

```bash
# 创建功能分支
git checkout -b feature/user-registration

# 在功能分支上开发并提交
git add src/auth/registration/*
git commit -m "feat(auth): 实现用户注册表单"

git add src/auth/registration/validation.js
git commit -m "feat(auth): 添加注册表单客户端验证"

# 定期从主分支同步最新代码
git fetch origin
git rebase origin/main

# 功能完成后，创建合并请求
# GitHub/GitLab UI操作或使用CLI工具

# 代码审查通过后合并到主分支
git checkout main
git merge --no-ff feature/user-registration
git push origin main

# 发布版本
git tag -a v1.2.0 -m "版本1.2.0"
git push origin v1.2.0
```

**标准Git分支模型图示**:

```
main    ───────────●─────────────●────────────●───────
          ↑         ↑             ↑            ↑
          │         │             │            │
release  ─●─────────●─────────────●────────────●───────
          ↑         ↑             ↑            ↑
          │         │             │            │
develop  ─●─────────●─────────────●────────────●───────
         /│\        ↑            /│\           ↑
        / │ \       │           / │ \          │
feature ●─●──●─────●●          ●──●──●────────●●
```

## 4. 代码质量检查清单

使用以下检查清单评估代码质量，并在代码审查过程中作为参考。

| 质量项 | 质量要求 | 是否适用 | 自检结果 | 审核管理 | 审核结果 | 问题说明 | 问题等级 | 修改说明 | 验证结果 |
|-------|---------|--------|---------|---------|---------|---------|--------|---------|---------|
| **命名规范** | | | | | | | | | |
| 变量命名 | 名称准确描述内容，符合命名规范，长度在2-20字符内 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 函数命名 | 以动词开头，准确描述功能，长度在2-30字符内 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 类命名 | 使用名词，遵循Pascal命名法，长度在2-30字符内 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 常量命名 | 全大写，下划线分隔单词 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| **代码结构** | | | | | | | | | |
| 文件长度 | 不超过500行，超过需合理分解 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 函数长度 | 不超过50行，超过需合理分解 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 函数参数 | 参数数量不超过5个 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 类方法数量 | 公共方法不超过20个 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 嵌套深度 | 控制语句嵌套不超过3层 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 代码重复 | 重复代码段(>10行)不超过2处 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| **注释文档** | | | | | | | | | |
| 注释比例 | 注释行占比10%-30% | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 复杂算法注释 | 复杂算法有流程说明和复杂度分析 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| API文档 | 公共API有完整参数、返回值、异常说明 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 模块文档 | 每个模块有README说明用途和用法 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| **错误处理** | | | | | | | | | |
| 异常捕获 | 所有可预见异常都有处理逻辑 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 异常信息 | 异常包含错误码、信息、位置和上下文 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 资源释放 | 所有资源分配在try-finally或等效结构中 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 输入验证 | 所有外部输入经过类型和范围验证 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| **测试质量** | | | | | | | | | |
| 测试用例数量 | 每个公共方法至少1个正向和1个异常测试 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 行覆盖率 | 至少80%的代码行被测试覆盖 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 分支覆盖率 | 至少70%的分支被测试覆盖 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 核心逻辑覆盖率 | 核心业务逻辑至少90%覆盖率 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| **性能指标** | | | | | | | | | |
| 时间复杂度 | 算法复杂度与数据规模匹配 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 数据库操作 | 循环中无数据库操作，查询结果有分页 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 资源释放 | 大对象使用后显式释放，连接使用后关闭 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 缓存策略 | 缓存有明确过期策略和容量限制 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| **安全编码** | | | | | | | | | |
| SQL注入防护 | 使用参数化查询，无字符串拼接SQL | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| XSS防护 | 输出到HTML的内容进行安全转义 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 密码存储 | 使用bcrypt等算法，工作因子≥10 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 敏感数据加密 | 敏感数据使用AES-256等强加密 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| **版本控制** | | | | | | | | | |
| 提交粒度 | 每次提交只包含一个逻辑变更 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 提交消息 | 符合规定格式：类型(范围): 描述 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 分支策略 | 遵循分支管理规定，特性分支生命周期<2周 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |
| 合并前检查 | 合并前通过所有自动化测试和代码审查 | □ | □符合 □不符合 | | □符合 □不符合 | | □高 □中 □低 | | □已修复 □未修复 |

## 5. 实施与落地

### 5.1 代码审查流程

1. **自检阶段**：开发人员使用自动化工具检查并根据检查清单对自己的代码进行自检
2. **提交检查**：代码提交时触发自动构建和测试，检查代码是否符合基本要求
3. **同行审查**：至少1名同级开发人员进行代码审查，重点关注业务逻辑和设计
4. **技术领导审查**：架构变更和核心模块由技术领导进行额外审查
5. **反馈与修正**：开发人员在2个工作日内修复审查中发现的问题
6. **验证与合并**：审查者验证修改结果后批准合并代码

### 5.2 工具支持

- **静态代码分析**：SonarQube配置项目质量阈(如复杂度<15，重复率<5%)
- **代码风格检查**：ESLint/PMD等强制执行代码风格规范
- **单元测试框架**：配置覆盖率目标(80%)和测试执行时间限制
- **构建系统**：Jenkins/GitHub Actions配置自动化质量门禁
- **代码度量**：定期(每周)生成代码质量趋势报告

### 5.3 培训与改进

- 每季度组织一次代码质量培训，分享典型问题和最佳实践
- 建立代码质量评分机制，与绩效考核挂钩
- 设立"代码质量改进小组"，定期(每季度)回顾并更新质量标准
- 设立最佳代码奖励机制，鼓励团队成员提升代码质量

## 6. 问题等级定义

- **高级问题**：
  - 导致系统崩溃或数据损坏的缺陷
  - 存在明确的安全漏洞(如SQL注入、XSS等)
  - 会导致性能下降50%以上的问题
  - 违反核心架构设计原则的代码

- **中级问题**：
  - 影响系统部分功能但不导致崩溃的缺陷
  - 代码重复率超过10%的情况
  - 违反项目编码规范的主要条款
  - 测试覆盖率低于目标的核心模块
  - 会导致性能下降20%-50%的问题

- **低级问题**：
  - 不影响功能但不符合最佳实践的代码
  - 文档或注释不完整但代码功能正确
  - 命名不够精确但基本符合规范
  - 代码格式不一致但不影响可读性
  - 轻微的代码重复或性能影响低于20%

## 7. 附录

### 7.1 语言特定编码规范

- **Java编码规范**：
  - 类名使用PascalCase，方法和变量名使用camelCase
  - 包名全部小写，使用反向域名格式(com.company.project)
  - 使用Optional代替null返回值
  - 优先使用不可变集合和对象

**Java代码示例**:

```java
// 良好实践
package com.company.project.user;

import java.util.Collections;
import java.util.List;
import java.util.Optional;

public class UserService {
    private final UserRepository userRepository;
    
    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }
    
    public Optional<User> findUserById(Long id) {
        // 返回Optional而非null
        return userRepository.findById(id);
    }
    
    public List<User> getActiveUsers() {
        List<User> users = userRepository.findByStatus("ACTIVE");
        // 返回不可变集合
        return Collections.unmodifiableList(users);
    }
}
```

- **JavaScript/TypeScript编码规范**：
  - 使用ES6+语法，避免var关键字
  - 异步代码优先使用async/await而非回调
  - 使用===而非==进行比较
  - 使用解构赋值提高代码可读性

**JavaScript/TypeScript代码示例**:

```typescript
// 不良实践
var getUserData = function(userId, callback) {
    fetch('/api/users/' + userId)
        .then(function(response) {
            return response.json();
        })
        .then(function(data) {
            if (data.status == 'active') {  // 使用 == 而非 ===
                callback(null, data);
            } else {
                callback('User not active', null);
            }
        })
        .catch(function(error) {
            callback(error, null);
        });
};

// 良好实践
const getUserData = async (userId: string): Promise<UserData> => {
    try {
        const response = await fetch(`/api/users/${userId}`);
        const data = await response.json();
        
        if (!response.ok) {
            throw new Error(`API错误: ${response.status}`);
        }
        
        if (data.status === 'active') {  // 使用 === 进行严格比较
            return data;
        } else {
            throw new Error('用户未激活');
        }
    } catch (error) {
        logger.error(`获取用户数据失败: ${error.message}`);
        throw error;
    }
};

// 良好实践：解构赋值
const { id, name, email } = user;
const [first, ...rest] = items;

// 良好实践：默认参数和箭头函数
const createUser = ({ name, email, role = 'user' }) => {
    // ...
};
```

- **Python编码规范**：
  - 遵循PEP 8风格指南
  - 使用类型注解增强代码可读性
  - 使用列表推导式代替for循环(适当情况下)
  - 异常处理必须指定具体异常类型，避免捕获Exception

**Python代码示例**:

```python
# 不良实践
def process_user_data(user_data):
    result = []
    for item in user_data:
        if item['status'] == 'active':
            result.append(item['name'])
    return result

# 良好实践: 使用类型注解和列表推导式
from typing import List, Dict, Any, Optional

def process_user_data(user_data: List[Dict[str, Any]]) -> List[str]:
    """处理用户数据并返回活跃用户名列表。
    
    Args:
        user_data: 包含用户信息的字典列表
        
    Returns:
        活跃用户的名称列表
    """
    return [item['name'] for item in user_data if item['status'] == 'active']

# 不良实践: 过于宽泛的异常捕获
def get_user_from_database(user_id):
    try:
        # 数据库操作
        return db.query(f"SELECT * FROM users WHERE id = {user_id}")
    except:  # 捕获所有异常
        return None

# 良好实践: 具体的异常处理
from database.exceptions import DatabaseConnectionError, QueryError

def get_user_from_database(user_id: int) -> Optional[User]:
    try:
        # 使用参数化查询
        return db.query("SELECT * FROM users WHERE id = %s", (user_id,))
    except DatabaseConnectionError as e:
        logger.error(f"数据库连接错误: {e}")
        raise ServiceUnavailableError("数据库服务暂时不可用") from e
    except QueryError as e:
        logger.error(f"查询执行错误: {e}")
        return None
```

- **C/C++编码规范**：
  - 使用智能指针管理资源，避免裸指针
  - 遵循RAII原则(资源获取即初始化)
  - 函数参数使用引用而非指针(除非需要表示可选参数)
  - 使用const修饰不变对象和方法

**C++代码示例**:

```cpp
// 不良实践
class User {
private:
    char* name;
    int age;
    
public:
    User(const char* n, int a) {
        name = new char[strlen(n) + 1];
        strcpy(name, n);
        age = a;
    }
    
    ~User() {
        delete[] name;  // 手动资源管理容易出错
    }
    
    void updateAge(int* newAge) {  // 使用指针作为参数
        if (newAge != NULL) {
            age = *newAge;
        }
    }
};

// 良好实践
#include <memory>
#include <string>

class User {
private:
    std::string name;  // 使用标准库字符串，自动管理内存
    int age;
    
public:
    User(std::string n, int a) : name(std::move(n)), age(a) {}
    // 不需要析构函数，自动管理资源
    
    void updateAge(int newAge) {  // 直接传值，更安全清晰
        age = newAge;
    }
    
    // 常量方法，不修改对象状态
    std::string getName() const {
        return name;
    }
    
    int getAge() const {
        return age;
    }
};

// 资源管理良好实践
void processFile(const std::string& filename) {
    // 使用智能指针自动管理文件资源
    auto file = std::make_unique<std::ifstream>(filename);
    if (!file->is_open()) {
        throw std::runtime_error("无法打开文件: " + filename);
    }
    
    // 文件处理...
    // 函数结束时文件自动关闭
}
```

- **SQL编码规范**：
  - 关键字使用大写，表名和列名使用小写
  - 复杂查询使用CTE(WITH语句)提高可读性
  - 表名使用单数形式
  - 显式指定列名，避免SELECT *

**SQL代码示例**:

```sql
-- 不良实践
select * from users u 
join orders o on u.id = o.user_id 
where u.status = 'active' and o.created_at > '2023-01-01';

-- 良好实践: 格式化和命名约定
SELECT 
    u.id, 
    u.username, 
    u.email,
    o.order_id, 
    o.total_amount
FROM 
    user u
JOIN 
    order o ON u.id = o.user_id
WHERE 
    u.status = 'active' 
    AND o.created_at > '2023-01-01';

-- 良好实践: 使用CTE提高复杂查询的可读性
WITH active_users AS (
    SELECT id, username, email
    FROM user
    WHERE status = 'active'
),
recent_orders AS (
    SELECT user_id, COUNT(*) as order_count, SUM(total_amount) as total_spent
    FROM order
    WHERE created_at > '2023-01-01'
    GROUP BY user_id
)
SELECT 
    u.username,
    u.email,
    COALESCE(o.order_count, 0) AS order_count,
    COALESCE(o.total_spent, 0.00) AS total_spent
FROM 
    active_users u
LEFT JOIN 
    recent_orders o ON u.id = o.user_id
ORDER BY 
    o.total_spent DESC;
```

### 7.2 参考资源

- [Clean Code: A Handbook of Agile Software Craftsmanship (Robert C. Martin)](https://www.amazon.com/Clean-Code-Handbook-Software-Craftsmanship/dp/0132350882)
- [Effective Java (Joshua Bloch)](https://www.amazon.com/Effective-Java-Joshua-Bloch/dp/0134685997)
- [Code Complete: A Practical Handbook of Software Construction (Steve McConnell)](https://www.amazon.com/Code-Complete-Practical-Handbook-Construction/dp/0735619670)
- [OWASP安全编码实践](https://owasp.org/www-project-secure-coding-practices-quick-reference-guide/)
- [The Pragmatic Programmer (Andrew Hunt & David Thomas)](https://www.amazon.com/Pragmatic-Programmer-Journeyman-Master/dp/020161622X)
- [重构：改善既有代码的设计 (Martin Fowler)](https://www.amazon.com/Refactoring-Improving-Design-Existing-Code/dp/0201485672)
